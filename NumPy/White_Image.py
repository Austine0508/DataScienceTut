import cv2
import numpy as np

array=np.zeroes([500,500,3]) # Creates an array with 500 matrices of 500 rows and 3 columns with 0 as values
# In our work here, above line indicates, 500x500 pixels and 3 refers to the color channel RGB (red, green, blue -3)

